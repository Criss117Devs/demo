<ul class="ul-vertical bg-white-1" role="menu" aria-hidden="true" aria-label="${category.id}">
    <isloop items="${menuItem.subCategories}" var="category">
        <isif condition="${category.subCategories}">
        <iselse/>
            <li role="presentation">
                <a href="${category.url}" class="menu-items-desktop-ul-li-a-submenu-items a1" id="${category.id}" role="menuitem" tabindex="0">${category.name}</a>
            </li>
        </isif>
    </isloop> 
</ul>